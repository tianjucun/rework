    
    Generator 与函数上下文的关系

    传统上下文执行关系, 当调用一个函数时, 会在上下文环境栈中加入当前函数的上下文环境, 等当前函数执行完, 
    
    就会删除当前函数的上下文环境, 并交给上一层环境处理

    对于 Generator 而言: 执行上下文环境, 遇到 yield 命令, 就会暂时退出堆栈, 但并不是消失, 
    
    里面的所有变量和对象会冻结在当前状态。等到对它执行next命令时，这个上下文环境又会重新加入调用栈，冻结的变量和对象恢复执行。


    

